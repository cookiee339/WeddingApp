-- Development seed data for Wedding Photo App
-- This file is loaded when starting the database in development mode

-- Insert some sample access tokens for testing
-- Note: The backend will create the tables automatically, but we can insert some test data

-- Wait for tables to be created by the backend
-- This is just a placeholder file - the backend handles table creation

-- Sample development data (uncomment if needed for testing)
-- INSERT INTO access_tokens (token, description, created_at, expires_at, is_active) 
-- VALUES 
-- ('dev-token-table1', 'Development Table 1', NOW(), NOW() + INTERVAL '48 hours', true),
-- ('dev-token-table2', 'Development Table 2', NOW(), NOW() + INTERVAL '48 hours', true),
-- ('dev-token-family', 'Development Family Access', NOW(), NOW() + INTERVAL '72 hours', true);

-- This file ensures the volume mount works correctly
-- Add any development-specific database setup here